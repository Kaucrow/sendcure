use crate::{
    prelude::*,
    HELP_TEXT,
    model::{
        common::TimeoutType,
        input::InputMode,
        screens,
        Popup,
    },
    ui::centered_rect,
};

pub fn render(app: &App, state: &screens::counter::State, f: &mut Frame) -> Result<()> {
    Ok(())
}